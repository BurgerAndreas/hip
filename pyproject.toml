[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hip"
version = "0.1.0"
description = ""
authors = [
    {name = "Andreas Burger", email = "andreas.burger@mail.utoronto.ca"},
]
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy",
    "scipy",
    "pytest<9",
    "pandas",
    "matplotlib",
    "ase",
    "rdkit>=2024.3.6",
    "ruff",
    "tqdm",
    "pyarrow",
    "lightning==2.6.1",
    "e3nn==0.6.0",
    "omegaconf",
    "hydra-core",
    "pyyaml",
    "wandb",
    "kagglehub>=0.3.12",
    "lmdb==1.5.1",
    "muon-optimizer @ git+https://github.com/KellerJordan/Muon",
    "datasets>=4.4.1",
]

[project.optional-dependencies]
cuda121 = [
    "torch==2.5.1",
    "torch-geometric==2.7.0",
    "pyg-lib==0.4.0+pt25cu121",
    "torch-scatter==2.1.2+pt25cu121",
    "torch-sparse==0.6.18+pt25cu121",
    "torch-cluster==1.6.3+pt25cu121",
]
cuda126 = [
    "torch==2.11.0",
    "torch-geometric==2.7.0",
    "pyg-lib==0.6.0+pt211cu126",
    "torch-scatter==2.1.2+pt211cu126",
    "torch-sparse==0.6.18+pt211cu126",
    "torch-cluster==1.6.3+pt211cu126",
]

[dependency-groups]
cuda121 = [
    "torch==2.5.1",
    "torch-geometric==2.7.0",
    "pyg-lib==0.4.0+pt25cu121",
    "torch-scatter==2.1.2+pt25cu121",
    "torch-sparse==0.6.18+pt25cu121",
    "torch-cluster==1.6.3+pt25cu121",
]

[tool.setuptools.packages.find]
include = ["hip*", "nets*", "ocpmodels*", "scripts*"]

[tool.uv.sources]
torch = [
  { index = "pytorch-cu121", group = "cuda121", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
  { index = "pytorch-cu121", extra = "cuda121", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
  { index = "pytorch-cu126", extra = "cuda126", marker = "sys_platform == 'linux' or sys_platform == 'win32'" },
]

[tool.uv]
default-groups = ["cuda121"]
conflicts = [
  [
    { extra = "cuda121" },
    { extra = "cuda126" },
  ],
  [
    { group = "cuda121" },
    { extra = "cuda126" },
  ],
]
find-links = [
  "https://data.pyg.org/whl/torch-2.5.1+cu121.html",
  "https://data.pyg.org/whl/torch-2.11.0+cu126.html",
]

[tool.uv.extra-build-dependencies]
torch-cluster = ["torch", "setuptools", "wheel"]
torch-scatter = ["torch", "setuptools", "wheel"]
torch-sparse = ["torch", "setuptools", "wheel"]
pyg-lib = ["torch", "setuptools", "wheel"]

[[tool.uv.index]]
name = "pytorch-cu121"
url = "https://download.pytorch.org/whl/cu121"
explicit = true

[[tool.uv.index]]
name = "pytorch-cu126"
url = "https://download.pytorch.org/whl/cu126"
explicit = true

[tool.ruff]
lint.ignore = ["F541"] 

[tool.pytest.ini_options]
filterwarnings = [
    'ignore:.*torch_geometric\.distributed.*deprecated.*:DeprecationWarning',
    "ignore:The 'repr' attribute with value False was provided to the `Field\\(\\)` function.*",
    "ignore:The 'frozen' attribute with value True was provided to the `Field\\(\\)` function.*",
    "ignore:Can't initialize NVML:UserWarning",
]
